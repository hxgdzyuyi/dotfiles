[alias]
  st = status -bs
  dc = diff --cached
  cam = commit -ave -F /Users/qingyang/.commit-for-git
  ca = commit -av
  br = branch
  amend = commit --amend -C HEAD
  reword = commit --amend
  rv = checkout HEAD --force --
  ff = merge --ff-only
  graf = log --graph --oneline --decorate
  nb = checkout --track HEAD -b
  tip = log -1 --stat
  sw = checkout
  co = checkout
  rh = reset --hard
  ph = push origin HEAD
  svr = svn rebase
  svd = svn dcommit

[color]
  diff = auto
  status = auto
	ui = auto

[core]
  pager = less -+FSRX -FRX
	autocrlf = false
	filemode = false
	deltaBaseCacheLimit = 1G

[user]
	name = hxgdzyuyi
	email = hxgdzyuyi@gmail.com
[credential]
	helper = cache --timeout=3600
[push]
	default = matching
